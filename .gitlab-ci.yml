stages:
  - build
  - test
#  - deploy

job_build:
  stage: build
  script:
    - docker-compose build

job_test:
  stage: test
  dependencies:
    - build
  script:
    - docker-compose up -d
# Backend testing for both ruby and python
  after_script:
    - docker-compose down

#job_deploy:
#  stage: deploy
#  only:
#    - main
#  script:
#    - echo "Deploying to production environment"
##    SSH into EC2/cloud instance
#    - docker-compose up -d
